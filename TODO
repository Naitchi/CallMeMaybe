-/ Faire des fonctions reutilisable, notamment pour recuperer le resultat de la llm
Se renseigner sur UV
Faire une fonction pour recuperer les fonctions dans le fichier JSON sans le return 
Faire le prompt qui va s'addition a celui de l'utilisateur. (donne moi du JSON sort moi la fonction qui colle, etc)
Faire une fonction pour savoir si il a fini de generer sa reponse: En comptant le nombre de bracket ouverte que fermer 
parsing sur le fichier definition et prompt pydantic et module JSON
argparse
balisage chatlm
dans le make je dois creer un dossier output dans data 

quand je prends le max token deja check ce que c'est dans le vocabulaire pour m'assurer que ca fait sens dans du JSON ? 

a la fin tester via venv pour tester les pyproject et que tout soit bien installer (pydantic, json, re)